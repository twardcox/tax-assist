#!/usr/bin/env sh
# Locate the CE bundle (.ce/ preferred, ce/ fallback) and dispatch.
# post-checkout is advisory - never block checkout.
for d in .ce ce; do
  if [ -f "$d/scripts/hook-runner.cjs" ]; then
    node "$d/scripts/hook-runner.cjs" post-checkout 1>&2
    exit 0
  fi
done
exit 0
